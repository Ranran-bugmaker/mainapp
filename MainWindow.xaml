<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:Base_Class="clr-namespace:mainapp.Base_Class" x:Class="mainapp.MainWindow"
        WindowStyle="None"
        AllowsTransparency="True"
        ContextMenu="{DynamicResource right}"
        mc:Ignorable="d"
        Title="MainWindow" Height="536.4" Width="868.2" Icon="Resources/RR.ico">
    <Window.Resources>
        <ContextMenu x:Key="right">
            <MenuItem x:Name="close" Header="退出" Click="close_Click"/>
            <MenuItem x:Name="mainD" Header="打开界面"  Click="mainD_Click"/>
        </ContextMenu>
    </Window.Resources>
    <Grid Margin="0,0,0,0">
        <Grid x:Name="maingrid"/>
        <hc:ListClock />
        <hc:Drawer  x:Name="Sidebar" Dock="Left" ShowMode="Push" >
            <Border Background="{DynamicResource RegionBrush}" Width="300" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}">
                <ListBox x:Name="BarMain" SelectionMode="Single" SelectedIndex="0" ItemsSource="{Binding sidebarlist}" SelectionChanged="BarMain_SelectionChanged">


                    <hc:Interaction.Triggers>
                        <hc:EventTrigger EventName="SelectionChanged">
                            <hc:InvokeCommandAction Command="{Binding SelectionChangedCmd}"
                             CommandParameter="{Binding ElementName=BarMain}" />
                        </hc:EventTrigger>
                    </hc:Interaction.Triggers>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding name}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </hc:Drawer>
        <TextBlock x:Name="testtext" HorizontalAlignment="Left" Margin="662,65,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Height="90" Width="90"/>
    </Grid>
</Window>
